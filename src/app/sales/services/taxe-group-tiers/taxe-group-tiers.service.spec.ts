import { TestBed, inject } from '@angular/core/testing';

import { TaxeGroupTiersService } from './sale-group-tiers.service';

describe('TaxeGroupTiersService', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [TaxeGroupTiersService]
        });
    });

    it('should be created', inject([TaxeGroupTiersService], (service: TaxeGroupTiersService) => {
        expect(service).toBeTruthy();
    }));
});
import { TestBed, inject } from '@angular/core/testing';

import { TaxeGroupTiersService } from './sale-group-tiers.service';

describe('TaxeGroupTiersService', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [TaxeGroupTiersService]
        });
    });

    it('should be created', inject([TaxeGroupTiersService], (service: TaxeGroupTiersService) => {
        expect(service).toBeTruthy();
    }));
});
